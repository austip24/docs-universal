import type { NextPage } from "next";
import { Header, Footer } from "../components/sections";

const Home: NextPage = () => {
	return (
		<div>
			<Header />
			<Footer />
		</div>
	);
};

export default Home;
